import * as admin from 'firebase-admin';

import setup from '../firebase-adminsdk.json';

// const params = {
//   projectId: setup.project_id,
//   privateKeyId: setup.private_key_id,
//   privateKey: setup.private_key,
//   clientEmail: setup.client_email,
//   clientId: setup.client_id,
//   authUri: setup.auth_uri,
//   tokenUri: setup.token_uri,
//   authProviderX509CertUrl: setup.auth_provider_x509_cert_url,
//   clientC509CertUrl: setup.client_x509_cert_url,
// };

// export const firebaseInit = admin.initializeApp({
//   credential: admin.credential.cert(params),
// });
